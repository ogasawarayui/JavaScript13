/* Q1 var、let、constの使用についてまとめてください。
varは再宣言、再代入ができるが、基本的には使用しない。
letは変数を宣言する時に使用する。再代入ができる。
constは定数を宣言する時に使用する。再宣言、再代入はできない。
*/
/* Q2 なぜvarを使わないのか理由をまとめてください。
気付かぬうちに同じ名前で変数を宣言し、値を上書きしてしまったり、巻き上げ時に思わぬバグを誘発してしまう恐れがあるから*/
/* Q3 定数とは?
値を後から自由に変更できる変数に対して、最初に代入した値を後から変更できない値のこと。*/

